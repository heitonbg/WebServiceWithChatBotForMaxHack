# LevelUp — Система управления задачами с интеграцией MAX

LevelUp — это современная система управления задачами, объединяющая чат-бота для мессенджера **MAX**, веб‑интерфейс на **React**, удобное API на **FastAPI** и интеллектуальный анализ продуктивности через **GigaChat**.

Приложение позволяет пользователю эффективно управлять личными и рабочими задачами, анализировать продуктивность, использовать Kanban‑доску, Pomodoro‑таймер и календарь энергии.

---

## 🚀 Основные возможности

* **Управление задачами**: создание, редактирование, удаление, завершение.
* **Иерархические задачи**: поддержка подзадач.
* **Календарь энергии**: отслеживание состояния и планирование.
* **Pomodoro таймер**: встроенный режим фокусировки.
* **Канбан-доска**: визуальное управление проектами.
* **AI‑анализ продуктивности (GigaChat)**.
* **Синхронизация между ботом и веб‑приложением**.
* **Профиль пользователя с достижениями**.

---

## 🧩 Архитектура проекта

```
project/
├── requirements.txt        # Зависимости Python
├── main.py                 # Точка входа приложения
├── .env                    # Переменные окружения (ПОМЕНЯТЬ ВАЖНО)
├── __init__.py
└── app/
    ├── __init__.py
    ├── bot_impl.py         # Реализация бота MAX
    ├── api.py              # FastAPI приложение
    ├── config.py           # Конфигурация
    ├── gigachat_client.py  # Клиент GigaChat AI
    ├── models.py           # Модели БД
    ├── services.py         # Бизнес-логика
└── web/                # React веб-интерфейс
    ├── tailwind.config.cjs
    ├── postcss.config.cjs
    ├── package.json
    ├── index.html
    └── src/
        ├── app.jsx
        |── config.js # URL ХОСТ БОТА
        ├── main.jsx
        └── index.css
```

---

## 🛠 Технологии

### Backend (Python)

* **FastAPI** — REST API
* **SQLAlchemy** — ORM
* **aiomax** — клиент для MAX
* **aiohttp** — HTTP клиент
* **uvicorn** — ASGI сервер

### Frontend (React)

* **React**
* **Vite** — ультрабыстрый сборщик
* **TailwindCSS** — стили
* **PostCSS**

### Другие

* **SQLite** — локальная база данных
* **Docker** — контейнеризация
* **GigaChat API** — AI‑аналитика

---

## ⚙️ Установка и запуск

### 1. Backend

```bash
# Создание виртуального окружения
python -m venv venv

# Активация
# Windows
venv\Scripts\activate
# Linux / Mac
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp env.txt .env

# Запуск приложения
python main.py
```

### 2. Frontend (React)

```bash
cd web
npm install
npm run dev
```

### 3. Запуск через Docker
**ВАЖНО** в config.js и .env ПОМЕНЯТЬ всё на свое, там указаны localhost и вы не войдете на сайт, потому-что есть проверка через MAX Bridge, используйте миниапп для этого
<img width="606" height="278" alt="изображение" src="https://github.com/user-attachments/assets/004f693f-d3d8-4e15-b7e6-60e33d7fb472" />

```bash
git clone https://github.com/heitonbg/WebServiceWithChatBotForMaxHack.git
sudo docker build -t my-hackathon-bot .
sudo docker run -p 3000:5173 -p 8000:8000 --env-file .env --name running-bot my-hackathon-bot
```


---

## 🔧 Переменные окружения (.env)

```
MAX_BOT_TOKEN=your_bot_token_here
DATABASE_URL=sqlite:///data.db
WEB_APP_URL=https://webtomax.vercel.app
GIGACHAT_CLIENT_SECRET=your_gigachat_secret
GIGACHAT_AUTH_DATA=your_gigachat_auth_data
```

---

## 📡 API Endpoints

### Основные

* `GET /` — статус API
* `GET /tasks/list` — список задач
* `POST /tasks/create` — создание задачи
* `POST /tasks/complete` — завершение задачи
* `POST /tasks/decompose` — разложение задачи на подзадачи (AI)
* `GET /user/profile` — профиль пользователя
* `GET /user/ai-analytics` — анализ продуктивности
* `GET /kanban/projects` — данные для Kanban

---

## 🤖 Команды MAX‑бота

* **/start** — регистрация, главное меню
* **/add [задача]** — создать задачу
* **/list_tasks** — список задач
* **/complete [id]** — завершить задачу
* **/decompose [текст]** — разложить на подзадачи
* **/analyze** — анализ дня
* **/motivation** — случайная мотивация

---

## 🗄 База данных

* SQLite
* Автоматическое создание таблиц
* Relations: пользователь → задачи → проекты
* Безопасность через ORM (защита от SQL‑инъекций)

---

## 📈 Логи и мониторинг

Приложение логирует:

* действия пользователей
* ошибки
* запросы к GigaChat
* синхронизацию между системами

Уровень логов: **INFO** и выше.

---

## 🔮 Расширение функциональности

Чтобы добавить новую возможность:

1. Добавьте модель в `models.py`.
2. Реализуйте бизнес‑логику в `services.py`.
3. Добавьте endpoint в `api.py`.
4. Обновите веб-интерфейс в React.
5. При необходимости добавьте команды в `bot_impl.py`.

---

## 📌 Итог

LevelUp — это масштабируемая система для продуктивной работы с задачами, сочетающая гибкий backend, удобный веб‑интерфейс и интеграцию с MAX. Идеально подходит для личного использования, командной работы и повышения продуктивности.
